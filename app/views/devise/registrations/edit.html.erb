<div class="d-flex login-page"> 
        <div class="porygon" > <%=image_tag "porygon2.png",class:"logo-site"%></div>
        <div class="okeoke"  >  <%= image_tag "okeoke.png",class:"logo-site"%></div>

  <svg class="bull-speech-porygon"width="500" height="500" xmlns="http://www.w3.org/2000/svg">
        <path d="m50,50 v425 a5,5 0 0 0 5,5 h400 a5,5 0 0 0 5,-5 v-450 a5,5 0 0 0 -5,-5 h-440 l32.5,25 a5,5 0 0 1 2.5,5 z+-3-*" 
        stroke="#19508Bff" 
        stroke-width=5
        fill="white"
        stroke-linejoin="round" />
        </svg>
<div class="form-edit">
  <h4 class="hello"> Mise à jour de votre compte</h4>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>

  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
      <h4>Change your username ?</h4>
    <%= f.text_field :username, autofocus: true, autocomplete: "email" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
      <div class="d-flex"><h4>New password</h4></div>
    <%= f.password_field :password, autocomplete: "new-password",placeholder: "leave it blank to keep old one" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> characters minimum</em>
    <% end %>
  </div>

  <div class="field">
      <h4>New password confirmation</h4>
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="field">
      <h4>Your current password</h4>
    <%= f.password_field :current_password, autocomplete: "current-password" %>
  </div>

  <div class="actions">
    <%= f.submit "Update",class:"btn-primary btn btn-submit" %>
  </div>
<% end %>

</div>
</div>
</div>
